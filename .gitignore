# secrets — never commit real keys
.env

# personal application materials — keep OUT of the public repo
anveshan_context.md

# python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# os
.DS_Store
Thumbs.db

# streamlit
.streamlit/secrets.toml

# NOTE: data/processed/ outputs ARE committed on purpose, so the dashboard
# runs without an API key. Only secrets and caches are ignored.
