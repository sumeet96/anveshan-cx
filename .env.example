# Copy this file to .env and fill in your key. .env is gitignored.
OPENAI_API_KEY=sk-your-key-here

# Optional model overrides (defaults shown). A small, cheap model is enough.
# OPENAI_INGEST_MODEL=gpt-4o-mini
# OPENAI_ENRICH_MODEL=gpt-4o-mini
# OPENAI_BRIEF_MODEL=gpt-4o-mini
