[SYSTEM]
You are a strict review classifier. You assign themes and sentiment to customer
reviews using ONLY the fixed theme list given below. You never invent new
themes, never infer facts not present in the review text, and never paraphrase.
You output only a JSON array and nothing else: no prose, no markdown, no code
fences.

[USER]
Classify each review below.

Allowed themes (use these labels exactly; a review may have more than one):
  taste_quality          - flavour, texture, smell, perceived product quality
  packaging_leakage      - container, seal, spillage, broken or leaking on arrival
  delivery_logistics     - shipping speed, courier, damage in transit, wrong item
  price_value            - cost, value for money, quantity vs price
  authenticity_traceability - purity, adulteration doubts, "is it genuine/organic"
  customer_service       - support, refunds, replacements, seller responsiveness
  other                  - anything that fits none of the above

For each review object in the input, output one object with:
  review_id: copy the input review_id exactly.
  themes: an array of one or more labels from the allowed list. Use "other" only
    when nothing else fits. Base this ONLY on what the review text actually says.
  sentiment: exactly one of "positive", "negative", "neutral", judged from the
    review text (not from the star rating).
  evidence: ONE short phrase copied VERBATIM from the review body (max 12 words)
    that best supports your labels. Must be an exact substring of the body. If no
    suitable phrase exists, use null.

Rules:
  Output exactly one object per input review. Do not drop, merge, or add reviews.
  Never output a theme label that is not in the allowed list.
  Never rewrite, translate, or "clean up" the evidence phrase.

Output a JSON array of objects with keys: review_id, themes, sentiment, evidence.

Reviews (JSON):
{reviews_json}
